# shared-utils

Framework-free TypeScript helpers usable from any app or lib (`@starter/shared-utils`).
No React, no DOM, no app-specific code. If a helper needs React, it belongs in `ui`
or a feature lib instead.

Run `pnpm nx test shared-utils` for the unit tests.
